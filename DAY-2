# in day 2 
always use extention .tf or yml.tf 
# how to enter value from user 




variable username {}


output printname {
     value = "hello, ${var.username}"
}
--------------------------------------
if u want to run the tf file on non-interactive mode 
 code:
variable username {}

output printname {
     value = "hello, ${var.username}"
}
run command like this
terraform plan -var "username=ankita"
